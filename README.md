# Install

```sh
pnpm install
```

# Structure

```sh
├── libs
└── workspaces
```

# Useful commands

- remove all folders includes nested folders inside monorepo

```sh
pnpm -r exec -- rm -rf <folder name>
```
